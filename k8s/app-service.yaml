apiVersion: v1
kind: Service
metadata:
  name: campusfit-service
  namespace: campusfit
spec:
  type: NodePort
  selector:
    app: campusfit-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
